# --- LOCAL DEVELOPMENT (DOCKER) ---
DB_USER=crawler_user
DB_PASSWORD=crawler_pass
DB_HOST=localhost
DB_PORT=5433
DB_NAME=stock_market_crawler_data

# --- CLOUD DEPLOYMENT (SUPABASE) ---
# Use the Supavisor pooler (port 6543) for serverless workloads — direct
# port 5432 caps at 60 connections on the free tier and won't survive Cloud
# Run cold-start storms.
# DATABASE_URL=postgresql://postgres.[PROJECT]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# DB pool sizing (defaults tuned for Supabase free tier behind Supavisor).
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10
# DB_STATEMENT_TIMEOUT_MS=30000

# Brapi (https://brapi.dev) token for CVM ticker → cd_cvm fallback resolution.
# Without it, the fallback is skipped silently (avoids 401 spam for unknown
# tickers — BDRs, FIIs, etc).
# BRAPI_TOKEN=

# --- API authentication ---
# Shared secret required on every request (X-API-Key) and on /_tasks/* (Bearer).
API_KEY=dev-api-key

# --- Cloud Tasks (production) / Emulator (local) ---
# Set GCP_PROJECT_ID to enable Cloud Tasks enqueueing. Locally, point
# CLOUD_TASKS_EMULATOR_HOST at the aertje/cloud-tasks-emulator service
# (docker-compose default: cloud-tasks-emulator:8123).
# GCP_PROJECT_ID=local-project
# CLOUD_TASKS_EMULATOR_HOST=cloud-tasks-emulator:8123
# CLOUD_TASKS_QUEUE=crawler-queue
# CLOUD_TASKS_LOCATION=us-central1
# CLOUD_RUN_URL=http://worker:8000

# Logging
LOG_LEVEL=INFO
# LOG_FORMAT controls the loguru sink: "human" (colored stderr, local dev)
# or "gcp" (newline-delimited JSON on stdout, picked up automatically by
# Cloud Run / GCE COS / Cloud Run Jobs for Cloud Logging ingestion).
LOG_FORMAT=human

# Observability / service identity (attached to every log line and OTel span).
# SERVICE_VERSION should be overridden in CI with the deployed git SHA.
SERVICE_NAME=stock-market-crawler
SERVICE_VERSION=dev
DEPLOYMENT_ENV=development
# GCP_PROJECT_ID falls back to GOOGLE_CLOUD_PROJECT (auto-set on Cloud Run/GCE).
# When set, log entries link to Cloud Trace via fully-qualified trace URIs.
# GCP_PROJECT_ID=

# --- OpenTelemetry tracing (requires `uv sync --extra observability`) ---
# Kill switch — leave false until the SDK extras are installed.
OTEL_ENABLED=false
# Backend: console (debug) | otlp (Tempo/Collector) | gcp (Cloud Trace direct).
OTEL_EXPORTER=console
# 1.0 = trace every request (dev). Use 0.05–0.10 in prod.
OTEL_SAMPLE_RATIO=1.0
# Standard OTel SDK env vars (read by the SDK, not by core.config):
# OTEL_EXPORTER_OTLP_ENDPOINT=http://tempo:4317
# OTEL_PYTHON_FASTAPI_EXCLUDED_URLS=/health,/metrics,/docs,/openapi.json,/

# --- CORS (PRODUCTION ONLY) ---
# Comma-separated list of allowed origins when ENV=production.
# Example: ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=
